### ESLint

This project uses [@antfu/eslint-config](https://github.com/antfu/eslint-config) for ESLint configuration. You can run the linter with the following commands:

- `<%= packageManager %> run lint`: Check and Automatically fix linting errors..

The configuration can be found in `<%= locals.eslintConfigFileName %>`.